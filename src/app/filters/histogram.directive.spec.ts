import { HistogramDirective } from './histogram.directive';

describe('HistogramDirective', () => {
  it('should create an instance', () => {
    const directive = new HistogramDirective();
    expect(directive).toBeTruthy();
  });
});
